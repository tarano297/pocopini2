# Django Settings
DEBUG=True
SECRET_KEY=your-secret-key-here
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (برای استفاده از PostgreSQL)
# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=pokopini_db
# DB_USER=pokopini_user
# DB_PASSWORD=your-password
# DB_HOST=localhost
# DB_PORT=5432

# Redis (اختیاری - برای کش)
# REDIS_URL=redis://localhost:6379/1

# Email Configuration - Gmail
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=noreply@pokopini.com
ADMIN_EMAIL=admin@pokopini.com

# برای استفاده از سرویس‌های دیگر:
# 
# Yahoo Mail:
# EMAIL_HOST=smtp.mail.yahoo.com
# EMAIL_PORT=587
#
# Outlook/Hotmail:
# EMAIL_HOST=smtp-mail.outlook.com
# EMAIL_PORT=587
#
# MailFa (ایرانی):
# EMAIL_HOST=smtp.mailfa.net
# EMAIL_PORT=587
#
# در حالت توسعه (نمایش در کنسول):
# EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
