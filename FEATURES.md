# ویژگی‌های جدید سایت

## ✨ ویژگی‌های اضافه شده:

### 1. پاپ‌آپ خوش‌آمدگویی (WelcomePopup)
- نمایش خودکار بعد از 3 ثانیه
- پیشنهاد 10% تخفیف برای اولین خرید
- فرم ثبت ایمیل
- ذخیره در localStorage (یکبار نمایش)

**استفاده:** خودکار در App.js

### 2. دکمه اشتراک‌گذاری (ShareButton)
- اشتراک در تلگرام
- اشتراک در واتساپ
- کپی لینک
- انیمیشن‌های جذاب

**استفاده:** در صفحه جزئیات محصول

### 3. محصولات اخیراً مشاهده شده (RecentlyViewed)
- ذخیره خودکار محصولات دیده شده
- نمایش 4 محصول آخر
- امکان پاک کردن همه
- ذخیره در localStorage

**استفاده:** در صفحه اصلی (Home)

### 4. دکمه واتساپ شناور (WhatsAppButton)
- دکمه شناور در گوشه صفحه
- انیمیشن pulse
- Tooltip راهنما
- لینک مستقیم به واتساپ

**استفاده:** خودکار در App.js

**تنظیمات:** شماره واتساپ را در `WhatsAppButton.jsx` تغییر دهید

### 5. انیمیشن‌های Scroll (ScrollAnimation)
- Fade in/out
- Slide animations
- Scale animations
- Rotate animations

**استفاده:**
```jsx
<ScrollAnimation animation="fade-up" delay={0.2}>
  <div>محتوای شما</div>
</ScrollAnimation>
```

## 📁 فایل‌های ایجاد شده:

```
frontend/src/
├── components/
│   ├── WelcomePopup.jsx
│   ├── ShareButton.jsx
│   ├── RecentlyViewed.jsx
│   ├── WhatsAppButton.jsx
│   └── ScrollAnimation.jsx
├── hooks/
│   └── useScrollAnimation.js
└── utils/
    └── recentlyViewed.js
```

## 🎨 تنظیمات:

### شماره واتساپ:
فایل: `frontend/src/components/WhatsAppButton.jsx`
```javascript
const whatsappNumber = '989123456789'; // شماره خود را وارد کنید
```

### تخفیف پاپ‌آپ:
فایل: `frontend/src/components/WelcomePopup.jsx`
```jsx
<span className="text-3xl font-bold text-coral">۱۰٪</span>
```

### تعداد محصولات اخیر:
فایل: `frontend/src/utils/recentlyViewed.js`
```javascript
const MAX_RECENT_PRODUCTS = 12;
```

## 🚀 نحوه استفاده:

همه ویژگی‌ها به صورت خودکار فعال هستند!

- پاپ‌آپ: بعد از 3 ثانیه نمایش داده می‌شود
- واتساپ: در گوشه پایین سمت چپ
- اشتراک‌گذاری: در صفحه محصول
- محصولات اخیر: در صفحه اصلی
- انیمیشن‌ها: در تمام صفحات

## 💡 نکات:

1. پاپ‌آپ فقط یکبار نمایش داده می‌شود
2. محصولات مشاهده شده در localStorage ذخیره می‌شوند
3. دکمه واتساپ همیشه در دسترس است
4. انیمیشن‌ها با scroll فعال می‌شوند

## 🎯 بهبودهای آینده:

- [ ] اتصال فرم ایمیل به backend
- [ ] آمار محصولات پربازدید
- [ ] پیشنهادات هوشمند
- [ ] چت آنلاین
