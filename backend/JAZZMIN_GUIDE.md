# 🎨 راهنمای پنل مدیریت زیبا (Jazzmin)

پنل مدیریت پوکوپینی با استفاده از **Django Jazzmin** به یک رابط کاربری مدرن و حرفه‌ای تبدیل شده است.

## ✨ ویژگی‌های جدید

### 1. طراحی مدرن و زیبا
- رابط کاربری Bootstrap 4 با تم Flatly
- آیکون‌های Font Awesome برای تمام بخش‌ها
- پشتیبانی کامل از RTL برای فارسی
- طراحی ریسپانسیو و موبایل‌فرندلی

### 2. منوی کناری پیشرفته
- آیکون‌های سفارشی برای هر بخش:
  - 👤 کاربران و احراز هویت
  - 📦 محصولات و دسته‌بندی‌ها
  - 🛒 سفارشات و پرداخت‌ها
  - ⭐ نظرات و بازخوردها
  - 📍 آدرس‌ها و ارسال

### 3. داشبورد بهبود یافته
- نمایش آمار و اطلاعات مهم
- دسترسی سریع به بخش‌های مختلف
- جستجوی پیشرفته در مدل‌ها

### 4. تنظیمات قابل سفارشی‌سازی
- تغییر تم رنگی (در حال حاضر: Flatly)
- تنظیمات منوی کناری
- سفارشی‌سازی نوار بالا

## 🚀 دسترسی به پنل

### آدرس پنل:
```
http://localhost:8000/secure-panel-2024/
```

یا اگر `.env` را تغییر داده‌اید:
```
http://localhost:8000/admin/
```

## 🎨 تم‌های موجود

می‌توانید تم پنل را در فایل `settings.py` تغییر دهید:

```python
JAZZMIN_UI_TWEAKS = {
    "theme": "flatly",  # تم فعلی
}
```

### تم‌های پیشنهادی:
- **flatly** - مدرن و تمیز (فعلی)
- **cosmo** - رنگ‌های روشن و دوستانه
- **lux** - لوکس و حرفه‌ای
- **materia** - طراحی متریال
- **minty** - سبز و تازه
- **pulse** - بنفش و مدرن
- **sandstone** - گرم و دوستانه
- **simplex** - ساده و کاربردی
- **united** - نارنجی و پرانرژی
- **yeti** - آبی و آرام

## 🔧 سفارشی‌سازی بیشتر

### تغییر لوگو
در `settings.py`:
```python
JAZZMIN_SETTINGS = {
    "site_logo": "images/logo.png",  # مسیر لوگو
    "login_logo": "images/logo-login.png",  # لوگو صفحه ورود
}
```

### تغییر رنگ‌ها
```python
JAZZMIN_UI_TWEAKS = {
    "navbar": "navbar-dark navbar-primary",  # نوار بالا
    "sidebar": "sidebar-dark-primary",  # منوی کناری
    "brand_colour": "navbar-primary",  # رنگ برند
}
```

### اضافه کردن لینک‌های سفارشی
```python
JAZZMIN_SETTINGS = {
    "topmenu_links": [
        {"name": "خانه", "url": "admin:index"},
        {"name": "سایت", "url": "/", "new_window": True},
    ],
}
```

## 📱 ویژگی‌های موبایل

- منوی همبرگری برای موبایل
- طراحی ریسپانسیو
- لمس‌پذیری بهینه

## 🎯 نکات مهم

1. **فایل‌های استاتیک**: بعد از هر تغییر در تنظیمات، دستور زیر را اجرا کنید:
   ```bash
   python manage.py collectstatic --noinput
   ```

2. **راه‌اندازی مجدد**: بعد از تغییر تنظیمات، سرور را مجدداً راه‌اندازی کنید.

3. **کش مرورگر**: اگر تغییرات را نمی‌بینید، کش مرورگر را پاک کنید (Ctrl+Shift+R).

## 🆘 رفع مشکلات

### پنل زیبا نمایش داده نمی‌شود
```bash
cd backend
python manage.py collectstatic --noinput
python manage.py runserver
```

### آیکون‌ها نمایش داده نمی‌شوند
- اتصال اینترنت را بررسی کنید (Font Awesome از CDN بارگذاری می‌شود)
- یا در تنظیمات `use_google_fonts_cdn` را `False` کنید

### خطای 404
- مطمئن شوید که به آدرس صحیح رفته‌اید: `/secure-panel-2024/` یا `/admin/`
- فایل `.env` را بررسی کنید

## 📚 منابع بیشتر

- [مستندات Jazzmin](https://django-jazzmin.readthedocs.io/)
- [تم‌های Bootswatch](https://bootswatch.com/)
- [آیکون‌های Font Awesome](https://fontawesome.com/icons)

---

**نکته**: این پنل فقط برای محیط توسعه طراحی شده. برای محیط تولید، حتماً تنظیمات امنیتی را بررسی کنید.
