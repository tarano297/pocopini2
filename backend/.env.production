# ⚠️ تنظیمات امنیتی برای محیط Production
# این فایل را کپی کنید به .env.production و مقادیر واقعی را وارد کنید

# Django Settings
DEBUG=False
SECRET_KEY=f2E6&d0No4nF(v5K2^GD0Qk#_2siMV(LCXwFHX_m8T0-pIWS#e
ALLOWED_HOSTS=pokopini.ir,www.pokopini.ir

# Database (PostgreSQL برای Production)
# ⚠️ اگر PostgreSQL ندارید، این خطوط را کامنت کنید تا از SQLite استفاده شود
# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=pokopini_production
# DB_USER=pokopini_user
# DB_PASSWORD=your-strong-database-password
# DB_HOST=localhost
# DB_PORT=5432

# Redis Cache (اختیاری - اگر Redis ندارید، کامنت کنید)
# REDIS_URL=redis://localhost:6379/1

# Email Configuration
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-specific-password
DEFAULT_FROM_EMAIL=noreply@pokopini.ir
ADMIN_EMAIL=admin@pokopini.ir

# CORS Settings
CORS_ALLOWED_ORIGINS=https://pokopini.ir,https://www.pokopini.ir

# Security Settings
SECURE_SSL_REDIRECT=True
SECURE_HSTS_SECONDS=31536000

# 🔐 Admin Panel Security
# تغییر URL پنل ادمین (به جای /admin/ از یک URL سفارشی استفاده کنید)
# ⚠️ این URL را به کسی نگویید و در جای امنی یادداشت کنید!
ADMIN_URL=pokopini-secure-management-panel-2024/

# 🛡️ IP Whitelist برای پنل ادمین
# فقط این IP ها می‌توانند به پنل ادمین دسترسی داشته باشند
# مثال: ADMIN_IP_WHITELIST=192.168.1.100,203.0.113.5,198.51.100.10
ADMIN_IP_WHITELIST=

# 📧 اطلاع‌رسانی تلاش‌های ناموفق ورود
ADMIN_LOGIN_ATTEMPT_NOTIFICATION=True
NOTIFY_ADMINS_ON_FAILED_LOGIN=3

# 🔑 Two-Factor Authentication (2FA)
TWO_FACTOR_ENABLED=True
TWO_FACTOR_FORCE_ADMIN=True

# 🔐 API Key برای عملیات حساس (اختیاری)
REQUIRE_API_KEY_FOR_WRITE=False
VALID_API_KEYS=your-api-key-1,your-api-key-2

# Rate Limiting
RATELIMIT_ENABLE=True
